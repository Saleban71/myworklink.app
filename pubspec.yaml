name: worklink
description: WorkLink — trusted work marketplace for Zambia's informal workforce.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  flutter_dotenv: ^5.1.0
  geolocator: ^12.0.0
  google_fonts: ^6.2.0
  image_picker: ^1.1.0
  intl: ^0.19.0
  firebase_core: ^3.1.0
  firebase_messaging: ^15.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
